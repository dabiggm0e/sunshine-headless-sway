---

# AGENTS.md Update Plan

## Goal
Update `AGENTS.md` to match the actual working codebase. The user's current setup (all repo files + live configs) is working perfectly with Sunshine/Moonlight. AGENTS.md currently contains several discrepancies vs. the real configuration.

## Files Modified
- `/home/moe/sunshine-headless-sway/AGENTS.md` (only file changed)

## Changes

### 1. Fix `adapter_name` render node example (CRITICAL — line 265)
**Current:** `adapter_name = /dev/dri/renderD128`  
**Change to:** `adapter_name = /dev/dri/renderD129`  
**Reason:** The repo template `sunshine/sunshine.conf` has `adapter_name = /dev/dri/renderD129`.

### 2. Rewrite Vulkan driver section (CRITICAL — lines 50-76)
**Current:** Entire `### NVENC Encoding on Multi-GPU Systems` section states both services must use `nvidia_icd.json`  
**Change to:** Document that `sunshine-headless.service` uses `radeon_icd.json` (AMD VCN encoder), which is the working config. Explain when NVIDIA ICD would be needed instead.

New content:
```markdown
### Vulkan Driver Selection for Sunshine Encoding

Sunshine's encoding backend runs via Vulkan and must use the correct Vulkan ICD for the GPU that will do the encoding. On multi-GPU systems, auto-selection can pick the wrong GPU.

**How it works on this setup:**
- `sway-sunshine.service` runs Sway on the NVIDIA GPU (`WLR_DRM_DEVICES=/dev/dri/renderD129`)
- `sunshine-headless.service` uses `VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/radeon_icd.json` so Sunshine encodes via the AMD GPU's VCN encoder
- This works because Sway renders on NVIDIA (for display) while Sunshine encodes on AMD (for streaming)

**When you'd need NVIDIA's Vulkan ICD for Sunshine:**
- If you want Sunshine to encode on the NVIDIA GPU (NVENC) instead of AMD (VCN), set `VK_ICD_FILENAMES` to `nvidia_icd.json` in `sunshine-headless.service`
- This is only needed if the AMD GPU lacks VCN support or you prefer NVENC quality

**Verify encoding backend:**
```bash
journalctl --user -u sunshine-headless.service -n 30 --no-pager | grep -iE "encoder|nvenc|vcn"
# AMD setup: Creating encoder [h264_vcn], [hevc_vcn], [av1_vcn]
# NVIDIA setup: Creating encoder [h264_nvenc], [hevc_nvenc], [av1_nvenc]
```
```

### 3. Update working commit (CRITICAL — lines 425-427)
**Current:** `Working commit: 2aeec6a36ffa1e8b6a9be1604b44b0fc62e3ceff`  
**Change to:** `Working commit: f8bba00397972d1b0200c0242c84f3c67db4e8a4`

### 4. Document Lutris scripts (IMPORTANT — insert after line 137)
Add two new subsections in `## Scripts in sway-sunshine/`:

```markdown
### `start-lutris-game.sh`
Launches a Lutris-managed game in the headless Sway session. Used via `detached` in apps.json entries. Handles:
- Sets `WAYLAND_DISPLAY=wayland-1` for the headless session
- Launches the game via the Lutris runtime in the headless session
- Accepts a Lutris app slug or game ID as argument

### `stop-lutris-game.sh`
Stops the Lutris-managed game running in the headless Sway session. Used as **prep-cmd.undo** for Lutris entries. Kills the game process and cleans up any leftover state.

> **Note:** `start-lutris-game.sh` and `stop-lutris-game.sh` are automatically installed by `install.sh`.
```

### 5. Document Lutris detached entry format (IMPORTANT — insert after line 200)
Add new subsection in `## apps.json Format & Conventions`:

```markdown
### Lutris detached entries

Lutris games use the `detached` field with `start-lutris-game.sh`, similar to Steam entries:

```json
{
  "name": "Game Name (Lutris)",
  "detached": ["/home/moe/.config/sway-sunshine/start-lutris-game.sh <slug_or_id>"],
  "prep-cmd": [
    {"do": "/home/moe/.config/sway-sunshine/restore-default-sink.sh", "undo": ""},
    {"do": "/home/moe/.config/sway-sunshine/set-resolution.sh", "undo": "/home/moe/.config/sway-sunshine/stop-lutris-game.sh"}
  ]
}
```

Lutris undo uses `stop-lutris-game.sh` as the resolution undo hook, matching the pattern of using the game-stop script as cleanup.
```

### 6. Fix Wayland display numbering contradiction (IMPORTANT — lines 97-114)
**Current:** Says "Fixed display numbering — never auto-detect" and "There should never be wayland-2 or higher"  
**Change to:** Remove absolute claims, recommend verification:

```markdown
## Wayland Display Layout

**Standard display numbering:**
- `wayland-0` = main desktop (physical monitor)
- `wayland-1` = headless Sway session (Sunshine streaming target)

**On most setups, the headless session uses `wayland-1`.** However, on KDE/CachyOS or other setups where SDDM or other components create additional Wayland sockets, the headless session may get `wayland-2` or higher. Always verify:

```bash
ls /run/user/$(id -u)/wayland-*
```

**Important for install.sh:** Both service files (`sway-sunshine.service` and `sunshine-headless.service`) hardcode `WAYLAND_DISPLAY=wayland-1`. The `start-steam-game.sh` script also hardcodes `WAYLAND_DISPLAY="wayland-1"`. If your headless session uses a different display number, update these files accordingly.

**Verification:** After any install or restart, always confirm:
```bash
ls /run/user/$(id -u)/wayland-*
grep WAYLAND_DISPLAY ~/.config/systemd/user/sway-sunshine.service
grep WAYLAND_DISPLAY ~/.config/systemd/user/sunshine-headless.service
```
```

### 7. Fix install.sh config preservation description (IMPORTANT — line 213)
**Current:** `- Preserves existing sunshine.conf and apps.json if they already exist`  
**Change to:**
```markdown
- Overwrites sunshine.conf from template on each run
- Preserves existing apps.json if it already exists, running auto-migration (prep-cmd updates, duplicate cleanup)
```

### 8. Fix KDE udev rule wording (IMPORTANT — line 215)
**Current:** `(GNOME rule is active; KDE rule is comment-only`  
**Change to:** `(GNOME rule is active; KDE variant is comment-only`

### 9. Remove `"cmd": ""` from Desktop entry example (MINOR — lines 187-196)
Remove the `"cmd": ""` line from the Desktop entry JSON example to match the template.

## Verification
After making changes:
1. `cat AGENTS.md | grep -n "renderD129"` — should show renderD129 in adapter_name examples
2. `cat AGENTS.md | grep -A5 "Vulkan Driver Selection"` — should document radeon_icd.json as the working config
3. `cat AGENTS.md | grep "Working commit"` — should show f8bba00
4. `cat AGENTS.md | grep -c "start-lutris-game.sh"` — should be >= 2 mentions
5. `cat AGENTS.md | grep -c "stop-lutris-game.sh"` — should be >= 1 mention
6. `cat AGENTS.md | grep "wayland-2"` — should only appear in verification context, not as absolute claim
7. `cat AGENTS.md | grep "Preserves existing"` — should no longer appear (replaced by new text)
8. Run `./install.sh` to ensure nothing breaks (dry-run or actual run)

## Working Commit Reference Update
The working commit reference at the end of the file should be updated from `2aeec6a` to `f8bba00`.
