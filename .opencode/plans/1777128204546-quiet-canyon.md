# Plan: Update AGENTS.md to Document Mistakes from Original AI-Generated Setup

## Background

The original headless Sway + Sunshine setup (Reddit post by Awkward-Location-234, generated with Claude) contained several mistakes that caused issues for users trying to replicate the setup. These mistakes were inherited by this repo's AGENTS.md through copy-paste from the original AI-generated content. This plan documents what went wrong so the orchestrator doesn't repeat them.

## Changes to `AGENTS.md`

### Change 1: Line 10 — Add missing source directory

**Current:**
```
- Source files live in: `systemd/`, `sway-sunshine/`, `sunshine/`, `pipewire/`, `udev/`
```

**New:**
```
- Source files live in: `systemd/`, `sway-sunshine/`, `sunshine/`, `pipewire/`, `udev/`, `LutrisToSunshine/`
```

### Change 2: Line 44 — Clarify Steam migration behavior

**Current:**
```
Launches a Steam game in the headless Sway session. **Migrates Steam from the main desktop if it's running there.** Handles:
```

**New:**
```
Launches a Steam game in the headless Sway session. **Kills ALL Steam processes system-wide** (including any running on the main desktop) before launching in the headless session. Handles:
```

### Change 3: Line 51 — Clarify stop-steam-game.sh behavior

**Current:**
```
Shuts down Steam in the headless session and cleans up IPC. Used as **prep-cmd.undo** for Steam entries.
```

**New:**
```
Shuts down ALL Steam processes system-wide and cleans up IPC. Used as **prep-cmd.undo** for Steam entries. Note: does NOT restart Steam on the main desktop.
```

### Change 4: Line 147 — Add Vulkan renderer alternative

**Current:**
```
- Headless Sway must be running with `WLR_BACKENDS=headless` and `WLR_RENDERER=gles2`
```

**New:**
```
- Headless Sway must be running with `WLR_BACKENDS=headless` and `WLR_RENDERER=gles2` (or `vulkan` for AMD / modern wlroots + NVIDIA)
```

### Change 5: After line 166 — New subsection documenting inherited mistakes

Insert the following new `###` subsection after "### What Does NOT Work on KDE Plasma Wayland" and before "### Prep-cmd Standardization":

```markdown
### Known Issues from Original Setup

The original Reddit post and GitHub repo were AI-generated and contain several mistakes. Documented here to prevent repeating them.

#### KDE `ID_INPUT` Stripping is Fundamentally Broken (Critical)

The udev rule `ENV{ID_INPUT}=""` strips input tags from Sunshine virtual devices, but this hides them from ALL consumers including libinput (which headless Sway uses). libinput reads udev properties and skips devices without `ID_INPUT`. The original setup incorrectly claimed "Devices remain accessible to headless Sway via libinput (which reads evdev directly)" — this is false.

**If input passthrough doesn't work:** The udev rule may have broken the virtual devices for libinput. Fix options (in order of preference):

1. **Remove the udev rule entirely.** The Sway config already handles input isolation via `input * events disabled` followed by explicit `input` enable rules for each Sunshine device. This is the simplest and most reliable approach — KWin won't receive events because Sway disables them at the compositor level.
2. **Use `seatd`** to separate the headless Sway session onto a different seat, so the main desktop's input grabs never reach it.
3. **Use KWin config filtering** (`kwinrules`) to exclude Sunshine virtual devices from KWin's input handling, without touching udev properties.

> **Note:** The GNOME `mutter-device-ignore` approach (used on GNOME/Mutter) does NOT have this problem — it targets Mutter specifically without stripping generic input properties.

#### GPU Vendor Support

Both AMD and NVIDIA GPUs are viable for game streaming:

- **AMD:** Mesa open-source drivers + VCN encoding via Vulkan. `WLR_RENDERER=vulkan` is preferred on modern wlroots versions; `gles2` works as fallback.
- **NVIDIA:** Proprietary or open-source (Nouveau) drivers + NVENC encoding via Vulkan. `WLR_RENDERER=gles2` is the safe default on older wlroots versions due to DRM atomic mode-setting issues; `vulkan` may work on wlroots >= 0.17 with recent driver versions.

The service template defaults to `WLR_RENDERER=gles2` for maximum compatibility. Change to `vulkan` if you have an AMD GPU or modern wlroots + NVIDIA setup and want better rendering performance.

#### `WLR_DRM_DEVICES` Hardcoded in Service Template

The service template has `WLR_DRM_DEVICES=/dev/dri/renderD128` hardcoded. On multi-GPU systems, this may point to the wrong GPU.

**To detect the correct render node:**
```bash
ls -la /dev/dri/renderD*
# Match the render node to the GPU you want for capture:
ls -la /sys/class/drm/card*/device/vendor  # check vendor ID
```

Then update `WLR_DRM_DEVICES` in `systemd/sway-sunshine.service` to the matching node. On single-GPU systems, `renderD128` is usually correct.

#### Steam Migration Kills All Steam Processes

`start-steam-game.sh` and `stop-steam-game.sh` kill ALL Steam processes system-wide (`pgrep -x steam`), not just the headless session's Steam. If Steam is running on the main desktop, it will be shut down and migrated to the headless session.

`stop-steam-game.sh` does NOT restart Steam on the main desktop — the comment saying it "restarts it on the main desktop" is outdated. The game session just ends with Steam fully stopped. Users running Steam on their main desktop should be aware that launching a game via Sunshine will terminate their desktop Steam session.
```

## Additional Changes to Repo Files

### Change 6: `udev/85-sunshine-input-isolation-kde.rules` — Fix Broken KDE Rule

**Problem:** The current rule strips `ID_INPUT`, `ID_INPUT_KEYBOARD`, `ID_INPUT_MOUSE`, and `ID_INPUT_TOUCHPAD` tags from Sunshine virtual devices. This is fundamentally broken because libinput reads these udev properties to enumerate input devices. Setting them to empty hides the devices from ALL consumers including headless Sway's libinput backend.

**Fix:** Replace with a comment-only rule that explains the correct approach:

```udev
# KDE Plasma does not have an equivalent to Mutter's mutter-device-ignore property.
# Stripping ID_INPUT tags (the old approach) hides devices from ALL consumers
# including libinput, which breaks headless Sway input passthrough.
#
# Input isolation is handled by the Sway config:
# - "input * events disabled" disables all physical devices
# - Specific Sunshine passthrough devices are enabled individually
#
# KWin may see the virtual devices but cannot grab them since Sway runs in its own
# session with separate input handling. If you still experience input leaking to KDE,
# use seatd to run headless Sway on a separate seat (see AGENTS.md for details).
```

### Change 7: `systemd/sway-sunshine.service` — Add WLR_DRM_DEVICES Comment

Add a comment above the WLR_DRM_DEVICES line explaining multi-GPU considerations:

```ini
# DRM render node for headless backend. On multi-GPU systems, verify this points to
# the correct GPU (use "ls -la /dev/dri/renderD*" and check vendor IDs).
# For single-GPU systems, renderD128 is usually correct.
Environment=WLR_DRM_DEVICES=/dev/dri/renderD128
```

### Change 8: `systemd/sway-sunshine.service` — Add Vulkan Renderer Comment

Add a comment above the WLR_RENDERER line explaining renderer choices:

```ini
# Rendering backend: gles2 is the safe default (works with older wlroots + NVIDIA).
# For AMD GPUs or modern wlroots + NVIDIA, try vulkan for better performance.
Environment=WLR_RENDERER=gles2
```

### Change 9: `sunshine/apps.json` — Add Version Field

Add `"version": 2` to the top of apps.json after the opening brace. The current template is missing this field even though AGENTS.md says it uses v2 format.

**Current (first few lines):**
```json
{
  "env": {
    "SWAYSOCK": "/run/user/1000/sway-sunshine.sock"
```

**New:**
```json
{
  "version": 2,
  "env": {
    "SWAYSOCK": "/run/user/1000/sway-sunshine.sock"
```

### Change 10: `install.sh` — Template SWAYSOCK in apps.json env block

**Problem:** The install.sh templates `/home/YOUR_USER/` paths in prep-cmd entries but does NOT template the `/run/user/1000/` path inside the `env.SWAYSOCK` field of apps.json. This means non-UID-1000 users will have a broken SWAYSOCK path.

**Fix:** After the existing sed command that templates prep-cmd paths (around line 170), add:
```bash
sed -i "s|/run/user/1000/|/run/user/${UID}/|g" "${HOME}/.config/sunshine/apps.json"
```

This ensures the SWAYSOCK path in the env block is also templated for non-standard UIDs.

### Change 11: `install.sh` — Fix KDE Default Fallback

**Problem:** When DE detection fails, install.sh defaults to KDE (line 91). The KDE method is now known to be broken. The safer default would be GNOME's `mutter-device-ignore` approach since it's targeted and non-disruptive.

**Fix:** Change the default fallback from KDE to GNOME:
```bash
# Default to GNOME method (safer — targets Mutter specifically, doesn't break libinput)
# Users on KDE can manually switch to the KDE rule if needed.
DETECTED_DE="gnome"
```

Also update the comment above it accordingly.

## Verification

1. Read `AGENTS.md` after changes and verify:
    - Line 10 includes `LutrisToSunshine/` in source paths
    - Line 44 says "Kills ALL Steam processes system-wide" instead of "Migrates Steam"
    - Line 51 mentions "does NOT restart Steam on the main desktop"
    - Line 147 mentions `vulkan` as an alternative renderer
    - New subsection after line 166 documents all four known issues (KDE ID_INPUT, GPU support, WLR_DRM_DEVICES, Steam migration)
2. Verify no existing sections were deleted or renamed
3. File should be ~230 lines (up from 185)
4. Verify `udev/85-sunshine-input-isolation-kde.rules` contains only comments explaining why no active rule is needed
5. Verify `systemd/sway-sunshine.service` has comments about WLR_DRM_DEVICES and WLR_RENDERER choices
6. Verify `sunshine/apps.json` includes `"version": 2` field
7. Verify `install.sh` templates SWAYSOCK path in apps.json env block (not just prep-cmd paths)
8. Verify `install.sh` defaults to GNOME udev rule when DE detection fails
9. Verify AGENTS.md documents Wayland display numbering caveat for KDE/CachyOS
10. Verify seatd is listed as option 1 (not option 2) for KDE input isolation fixes
11. Verify NVIDIA encoder failure troubleshooting steps are documented
12. Verify `sg input` wrapper compatibility issue is documented

## CachyOS / KDE Plasma wlr Capture Additions

Multiple users on CachyOS (KDE Plasma Wayland) reported issues not covered in the original plan. These changes add documentation for KDE-specific wlr capture gotchas.

### Change 12: AGENTS.md — Add Wayland Display Numbering Caveat

**Location:** After the "Required setup" bullet list in "Capture Method: wlr" subsection (around line 148), before the blank lines.

Add a new paragraph:

```markdown
**Wayland display numbering on KDE/CachyOS:** On some KDE setups (especially CachyOS), SDDM or other components may create additional Wayland sockets, causing the headless session to get `wayland-2` instead of `wayland-1`. To verify the correct display number:
```bash
ls /run/user/$(id -u)/wayland-*
```
If the headless Sway session uses a different display number (e.g., `wayland-2`), update both `sway-sunshine.service` and `sunshine-headless.service` to match. The install script auto-detects this, but on KDE it may not always be correct — always verify after installation.
```

### Change 13: AGENTS.md — Promote seatd as Recommended KDE Input Isolation

**Location:** In the "KDE `ID_INPUT` Stripping is Fundamentally Broken" subsection (around line 176-180), replace the existing fix options with this updated list that promotes seatd to option 1:

Replace lines 176-180:
```markdown
**If input passthrough doesn't work:** The udev rule may have broken the virtual devices for libinput. Fix options (in order of preference):

1. **Use `seatd`** to separate the headless Sway session onto a different seat, so the main desktop's input grabs never reach it. This is the most reliable solution for KDE Plasma — proven working on CachyOS with dual GPU setups. Configure:
   - Install `seatd` package
   - Enable `seatd.service`
   - Change `LIBSEAT_BACKEND=noop` to `LIBSEAT_BACKEND=seatd` in `sway-sunshine.service`
   - Add `Environment=XDG_RUNTIME_DIR=/run/user/$(id -u)` if needed
2. **Remove the udev rule entirely.** The Sway config already handles input isolation via `input * events disabled` followed by explicit `input` enable rules for each Sunshine device. This works in most cases but KWin may still see the virtual devices (without grabbing them).
3. **Use KWin config filtering** (`kwinrules`) to exclude Sunshine virtual devices from KWin's input handling, without touching udev properties.
```

### Change 14: AGENTS.md — Add NVIDIA Encoder Failures on Bleeding-Edge Drivers

**Location:** After the "GPU Vendor Support" subsection (after line 191), add a new subsection:

```markdown
#### NVIDIA Encoder Failures on Bleeding-Edge Drivers

Users on bleeding-edge distros (CachyOS, Nobara) with recent NVIDIA drivers have reported NVENC encoder failures where all encoders (NVENC, VAAPI, software) fail at startup. Symptoms include:
```
[2026-XX-XX XX:XX:XX.XXX]: Info: Trying encoder [nvenc]
[2026-XX-XX XX:XX:XX.XXX]: Info: Encoder [nvenc] failed
[2026-XX-XX XX:XX:XX.XXX]: Info: Trying encoder [vaapi]
[2026-XX-XX XX:XX:XX.XXX]: Info: Encoder [vaapi] failed
[2026-XX-XX XX:XX:XX.XXX]: Fatal: Unable to find display or encoder during startup.
```

**Troubleshooting steps:**
1. **Verify Wayland display number** — The most common cause is Sunshine pointing at the wrong `WAYLAND_DISPLAY` (e.g., `wayland-2` instead of `wayland-1`). Check with `ls /run/user/$(id -u)/wayland-*`.
2. **Try `WLR_RENDERER=vulkan`** — Some NVIDIA driver versions work better with the Vulkan renderer than gles2. Update `WLR_RENDERER` in `sway-sunshine.service`.
3. **Check NVIDIA driver version** — Bleeding-edge drivers (e.g., 595+) may have regressions. Try rolling back to a stable driver version if issues persist.
4. **Verify Vulkan/VCN availability** — Run `vulkaninfo` and check that the NVIDIA Vulkan ICD is properly installed (`/usr/share/vulkan/icd.d/nvidia_icd.json` exists).
```

### Change 15: AGENTS.md — Add sg Input Wrapper Issue

**Location:** In the "Known Issues from Original Setup" section, add a new subsection after "Steam Migration Kills All Steam Processes" (after line 210):

```markdown
#### `sg input` Wrapper May Not Work on All Distributions

The `sunshine-headless.service` uses `ExecStart=/usr/bin/sg input -c /usr/bin/sunshine` to grant Sunshine access to the `input` group for device passthrough. On some distributions (notably Nobara and some CachyOS setups), this wrapper may fail because:
- The `sg` binary path differs (`/usr/bin/sg` vs other locations)
- The `input` group configuration differs from expectations

**If Sunshine fails to start with input-related errors:**
1. Check if `/usr/bin/sg` exists on your system
2. Try replacing `ExecStart=/usr/bin/sg input -c /usr/bin/sunshine` with `ExecStart=/usr/bin/sunshine` in `sunshine-headless.service` (Sunshine may already have the necessary permissions via udev rules)
3. Reload and restart: `systemctl --user daemon-reload && systemctl --user restart sunshine-headless.service`
```
